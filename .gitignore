# Secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build output
.next/
out/
dist/
build/

# Editor/OS
.vscode/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Launch kit — marketing assets (screenshots, video, logo exports, copy drafts).
# Kept on disk in this folder, deliberately not committed: large binaries and
# drafts that don't belong in the source repo history.
launch-kit/
